{
  "name": "aws-lambda-api",
  "type": "commonjs",
  "scripts": {
    "check": "serverless invoke local -f hello",
    "deploy": "serverless deploy --stage dev --verbose"
  },
  "dependencies": {
    "@middy/core": "^3.1.0",
    "@middy/http-json-body-parser": "^3.1.0",
    "aws-sdk": "^2.1186.0",
    "uuid": "^8.3.2"
  }
}
